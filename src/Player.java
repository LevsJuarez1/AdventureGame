
public class Player {

public String playerName;
int currentHealth;
int maxHealth;

public Player(String playerName, int currentHealth, int maxHealth){
	this.playerName = playerName;
	this.currentHealth = currentHealth;
	this.maxHealth = maxHealth;
}
}
